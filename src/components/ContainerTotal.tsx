import styled from "styled-components";

export const ContainerTotal = styled.main`
        position: relative;
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
`;